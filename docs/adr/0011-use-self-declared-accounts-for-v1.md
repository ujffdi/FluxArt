# Use self-declared accounts for V1

FluxArt V1 will allow users to register with self-declared credentials instead of requiring phone or email verification. This lowers signup friction while the product validates demand, but the system must still protect sessions, rate-limit abuse, and keep the door open for adding verified contact methods later.
