# Audit active image model configuration changes

FluxArt V1 will keep a minimal Model Configuration Change record whenever the Active Image Model Configuration is saved. The audit trail records who changed non-secret provider settings and when, because production generation failures, cost changes, and quality shifts need to be traceable without storing plaintext provider credentials.
