# Replace single active model with selectable model list

FluxArt will replace the single Active Image Model Configuration product concept with an admin-managed list of Selectable Image Models plus one Default Image Model. This supports paid-user model choice while preserving a deterministic default for Free Users and users who do not choose a model; task records continue to keep an Image Model Snapshot so later configuration changes do not rewrite history.
